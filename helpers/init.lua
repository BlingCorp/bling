return {
    client = require(... .. ".client"),
    color = require(... .. ".color"),
    filesystem = require(... .. ".filesystem"),
    icon_theme = require(... .. ".icon_theme"),
    shape = require(... .. ".shape"),
    time = require(... .. ".time"),
}
